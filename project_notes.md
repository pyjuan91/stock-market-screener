# Project Notes - Stock Market Screener

**合作模式**: 每次對話開始時，Gemini 會先讀取此筆記以了解當前進度。在每個任務完成後，會更新此筆記的內容。

**下次接續任務**: **美化 UI**: 改善整體版面配置與樣式。

最後更新：2025-09-25

## 目前專案狀態

- **專案已轉型為 Web 應用程式**
    - **後端 (Backend)**: 使用 `FastAPI` (`api.py`) 提供 API 服務，包含 `/api/scan` (取得最新數據) 和 `/api/history/{ticker}` (取得歷史數據) 兩個端點。
    - **前端 (Frontend)**: 使用 `React` 和 `Material-UI` 搭建。已安裝 `recharts` 用於圖表繪製。
    - **前後端整合**: 前端 UI 已能成功呼叫後端 API，將分析結果呈現在可互動的表格中。點擊表格中的股票可顯示其 RSI 和 MACD 歷史數據圖表。
- **核心功能**: 分析邏輯 (`stock_screener/`) 保持不變，能計算 MACD, RSI, 移動平均線, 和布林通道。

## 待辦事項 (To-Do)

- [ ] **改善 Web GUI**
    - [x] 建立 FastAPI 後端
    - [x] 建立 React 前端專案
    - [x] 完成前後端 API 串接
    - [x] **將分析結果以表格形式呈現**: 使用 Material-UI 的 Table 元件。
    - [x] **增加圖表**: 為關鍵指標增加視覺化圖表。
    - [ ] **美化 UI**: 改善整體版面配置與樣式。
- [ ] **在後端實現篩選邏輯**
    - [ ] 讓 API 可以接收篩選條件 (例如 RSI < 30)。
    - [ ] 後端根據條件過濾股票後再回傳結果。
- [ ] **錯誤處理**: 增強對於無效股票代碼或 API 請求失敗時的錯誤處理。

