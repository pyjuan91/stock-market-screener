# Project Notes - Stock Market Screener

**合作模式**: 每次對話開始時，Gemini 會先讀取此筆記以了解當前進度。在每個任務完成後，會更新此筆記的內容。

最後更新：2025-09-25

## 目前專案狀態

- **核心功能**: 專案現在可以透過**命令列參數**指定單一股票，獲取其歷史數據並計算 **MACD**, **RSI**, 和 **Moving Averages (SMA20, SMA50)** 技術指標。
- **架構**: `main.py` 負責處理使用者輸入，核心分析邏輯已遷移至 `stock_screener/screener.py`。
- **數據獲取**: 使用 `yfinance` 函式庫，封裝在 `stock_screener/data_fetcher.py`。
- **指標計算**: 使用 `pandas-ta` 函式庫，封裝在 `stock_screener/indicator_calculator.py`。

## 待辦事項 (To-Do)

- [x] **參數化股票代碼**: 將 `main.py` 中的股票代碼從硬編碼改為可由使用者命令列輸入。
- [x] **增加更多技術指標**:
    - [x] RSI (相對強弱指數)
    - [x] Moving Averages (移動平均線)
    - [ ] Bollinger Bands (布林通道)
- [ ] **建立篩選器 (Screener) 邏輯**:
    - [ ] 讀取一個股票清單（例如從一個 .txt 或 .csv 檔案）。
    - [ ] 遍歷清單中的所有股票，計算它們的技術指標。
    - [ ] 根據使用者設定的條件篩選出符合條件的股票。
- [ ] **改善輸出**:
    - [ ] 將篩選結果以表格形式清晰地呈現。
    - [ ] 考慮將結果儲存為 CSV 檔案。
- [ ] **錯誤處理**: 增強對於無效股票代碼或 API 請求失敗時的錯誤處理。

## 已知問題 / 待討論

- 如何定義篩選條件？是寫死在程式碼中，還是透過設定檔或命令列參數來動態指定？